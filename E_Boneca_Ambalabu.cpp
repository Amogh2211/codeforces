// Author: yeeter

#include <bits/stdc++.h>
using namespace std;
#define long int64_t

void solve()
{
    int n;
    cin >> n;
    std::vector<int> vec;
    for (int i = 0; i < n; ++i){
        int temp; cin >> temp;
        vec.push_back(temp);
    }

    
    
}

int main()
{
    ios::sync_with_stdio(false), cin.tie(nullptr);
    int T;
    cin >> T;
    while (T--) {
        solve();
    }
    return 0;
}
